CREATE TABLE products (
id INT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
name VARCHAR(512) NOT NULL,
description VARCHAR(1024),
create_date DATE NOT NULL,
place_storage INT NOT NULL,
reversed BOOLEAN,
PRIMARY KEY(id) 
);